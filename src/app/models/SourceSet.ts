export class SourceSet {
  ImageSource: string;
  MediaQuery: string;
  constructor(imageSource: string, mediaQuery: string) {
    this.ImageSource = imageSource;
    this.MediaQuery = mediaQuery;
  }
}
